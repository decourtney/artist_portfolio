{
  "name": "artist_portfolio",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.ts",
  "scripts": {
    "develop": "concurrently \"cd server && npm run develop\" \"cd client && npm run develop\"",
    "watch-css": "cd client && npx tailwindcss -i src/assets/input.css -o src/assets/index.css --watch",
    "install": "cd server && npm i && cd ../client && npm i",
    "seed": "cd server && npm run seed",
    "build": "cd client && npm run build"
  },
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@vitejs/plugin-react-swc": "^3.5.0",
    "vite": "^5.0.10",
    "vite-tsconfig-paths": "^4.2.2"
  }
}
